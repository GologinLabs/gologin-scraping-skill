# Article Extraction

Use this workflow when a page should stay in scraping mode.

1. Start with `webunlocker_markdown`.
2. Switch to `webunlocker_json` if headings or links are also needed.
3. Use `webunlocker_scrape` only when raw HTML matters.
4. Keep the flow stateless.
