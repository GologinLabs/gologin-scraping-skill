# Article Extraction

Use this workflow when a page should stay in scraping mode.

1. Start with `scraping_api_markdown`.
2. Switch to `scraping_api_json` if headings or links are also needed.
3. Use `scraping_api_scrape` only when raw HTML matters.
4. Keep the flow stateless.
