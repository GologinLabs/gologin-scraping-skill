# CLI Basic

Goal:
Fetch one page through the `gologin-scraping-api` CLI.

Prerequisite:

```bash
export GOLOGIN_SCRAPING_API_KEY="your_scraping_api_key"
```

Commands:

```bash
gologin-scraping-api text "https://example.com"
gologin-scraping-api json "https://example.com"
```
