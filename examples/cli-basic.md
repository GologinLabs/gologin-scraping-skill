# CLI Basic

Goal:
Fetch one page through the `gologin-webunlocker` CLI.

Prerequisite:

```bash
export GOLOGIN_WEBUNLOCKER_API_KEY="your_web_unlocker_key"
```

Commands:

```bash
gologin-webunlocker text "https://example.com"
gologin-webunlocker json "https://example.com"
```
